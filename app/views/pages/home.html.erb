<div class="jumbotron">
	<h1>Declare</h1>
	<p>An application for the Daily Declarative creative writing exercise.</p>
	<% if user_signed_in? %>
	<% else %>
		<p><%= link_to "Sign Up", new_user_registration_path, class: "btn-lg btn-primary" %></p>
	<% end %>
</div>